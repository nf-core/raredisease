nextflow_workflow {

    name "Test Workflow CALL_SNV"
    script "subworkflows/local/call_snv/main.nf"
    workflow "CALL_SNV"

    tag "subworkflows"
    tag "call_snv"
    tag "call_snv_deepvariant"
    tag "call_snv_sentieon"

    tag "gatk4_selectvariants"

    config "./nextflow.config"

    test("CALL_SNV - deepvariant wgs") {

        setup {
            run("GET_CHROM_SIZES") {
                script "modules/local/get_chrom_sizes/main.nf"
                process {
                    """
                    input[0] = channel.of([
                        [id:'genome'],
                        file(params.modules_testdata_base_path + 'genomics/homo_sapiens/genome/genome.fasta.fai', checkIfExists: true)
                    ])
                    """
                }
            }
        }

        when {
            workflow {
                """
                input[0]  = channel.of([[id:'interval'], []])
                input[1]  = channel.of([id:'justhusky'])
                input[2]  = channel.of([[:], []])
                input[3]  = channel.of([[id:'genome'], []])
                input[4]  = channel.of(file('https://raw.githubusercontent.com/nf-core/raredisease/refs/heads/master/assets/foundin.hdr', checkIfExists: true)).collect()
                input[5]  = channel.of(
                    [[id:'test', sample:'test', single_end:false, num_lanes:1, read_group: "'@RG\\\\tID:test\\\\tPL:illumina\\\\tSM:test'", lane:1, sex:2, phenotype:1, paternal:0, maternal:0, case_id:'justhusky'],
                     file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/test.paired_end.sorted.bam', checkIfExists: true),
                     file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/test.paired_end.sorted.bam.bai', checkIfExists: true)],
                    [[id:'test2', sample:'test2', single_end:false, num_lanes:1, read_group: "'@RG\\\\tID:test2\\\\tPL:illumina\\\\tSM:test2'", lane:1, sex:2, phenotype:1, paternal:0, maternal:0, case_id:'justhusky'],
                     file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/test2.paired_end.sorted.bam', checkIfExists: true),
                     file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/test2.paired_end.sorted.bam.bai', checkIfExists: true)]
                )
                input[6]  = GET_CHROM_SIZES.out.sizes.collect()
                input[7]  = channel.of([[id:'genome'], file(params.modules_testdata_base_path + 'genomics/homo_sapiens/genome/genome.fasta', checkIfExists: true)]).collect()
                input[8]  = channel.of([[id:'genome'], file(params.modules_testdata_base_path + 'genomics/homo_sapiens/genome/genome.fasta.fai', checkIfExists: true)]).collect()
                input[9] = channel.of([
                    [ id:'glnexus_config'],
                    file('../../../assets/glnexus_config_dp1.yml')
                ])
                input[10]  = channel.of([[id:'model'], []])
                input[11] = channel.of([[id:'par'], []]).collect()
                input[12] = channel.of([[id:'model'], []])
                input[13] = channel.of([[id:'target'], [], []])
                input[14] = 'wgs'
                input[15] = false
                input[16] = 'deepvariant'
                """
            }
        }

        then {
            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    path(workflow.out.genome_vcf[0][1]).vcf.variantsMD5,
                    workflow.out.deepvariant_report.collect { meta, report -> file(report).name },
                    workflow.out.genome_tabix.collect { meta, tbi -> file(tbi).name }
                ).match() }
            )
        }
    }

    test("CALL_SNV - deepvariant wgs, stub") {

        options "-stub"

        setup {
            run("GET_CHROM_SIZES") {
                script "modules/local/get_chrom_sizes/main.nf"
                process {
                    """
                    input[0] = channel.of([
                        [id:'genome'],
                        file(params.modules_testdata_base_path + 'genomics/homo_sapiens/genome/genome.fasta.fai', checkIfExists: true)
                    ])
                    """
                }
            }
        }

        when {
            workflow {
                """
                input[0]  = channel.of([[id:'interval'], []])
                input[1]  = channel.of([id:'justhusky'])
                input[2]  = channel.of([[:], []])
                input[3]  = channel.of([[id:'genome'], []])
                input[4]  = channel.of(file('https://raw.githubusercontent.com/nf-core/raredisease/refs/heads/master/assets/foundin.hdr', checkIfExists: true)).collect()
                input[5]  = channel.of(
                    [[id:'test', sample:'test', single_end:false, num_lanes:1, read_group: "'@RG\\\\tID:test\\\\tPL:illumina\\\\tSM:test'", lane:1, sex:2, phenotype:1, paternal:0, maternal:0, case_id:'justhusky'],
                     file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/test.paired_end.sorted.bam', checkIfExists: true),
                     file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/test.paired_end.sorted.bam.bai', checkIfExists: true)],
                    [[id:'test2', sample:'test2', single_end:false, num_lanes:1, read_group: "'@RG\\\\tID:test2\\\\tPL:illumina\\\\tSM:test2'", lane:1, sex:2, phenotype:1, paternal:0, maternal:0, case_id:'justhusky'],
                     file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/test2.paired_end.sorted.bam', checkIfExists: true),
                     file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/test2.paired_end.sorted.bam.bai', checkIfExists: true)]
                )
                input[6]  = GET_CHROM_SIZES.out.sizes.collect()
                input[7]  = channel.of([[id:'genome'], file(params.modules_testdata_base_path + 'genomics/homo_sapiens/genome/genome.fasta', checkIfExists: true)]).collect()
                input[8]  = channel.of([[id:'genome'], file(params.modules_testdata_base_path + 'genomics/homo_sapiens/genome/genome.fasta.fai', checkIfExists: true)]).collect()
                input[9] = channel.of([
                    [ id:'glnexus_config'],
                    file('../../../assets/glnexus_config_dp1.yml')
                ])
                input[10] = channel.of([[id:'model'], []])
                input[11] = channel.of([[id:'par'], []]).collect()
                input[12] = channel.of([[id:'model'], []])
                input[13] = channel.of([[id:'target'], [], []])
                input[14] = 'wgs'
                input[15] = false
                input[16] = 'deepvariant'
                """
            }
        }

        then {
            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    workflow.out
                ).match() }
            )
        }
    }
}
